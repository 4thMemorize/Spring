<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.memorize.dbtest.ExampleMapper">
    <insert id="insertExample" keyProperty="id">
        INSERT into example(name, tel, postcode) VALUES(#{name}, #{tel}, #{postcode})
    </insert>
</mapper>
